<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Imago.Views;assembly=Imago"
             xmlns:viewModels="clr-namespace:Imago.ViewModels;assembly=Imago" Title="Startseite"
             x:Class="Imago.Views.StartPage" Shell.NavBarIsVisible="False"
             BindingContext="{Binding StartPage, Source={StaticResource ViewModelLocator}}"
             x:DataType="viewModels:StartPageViewModel">
   
    <ContentPage.Content>
        <StackLayout>
            <Button Command="{Binding TestCharacterCommand}" Text="Testspieler" Margin="20"></Button>
            <Label Text="Welcome to Xamarin.Forms!"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>