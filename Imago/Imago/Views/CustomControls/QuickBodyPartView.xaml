<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             x:Class="Imago.Views.CustomControls.QuickBodyPartView" x:Name="QuickBodyPartViewControl">
    <ContentView.Content>
        <Frame BorderColor="Transparent" CornerRadius="0" VerticalOptions="Center">
            <Grid HorizontalOptions="Center" Padding="-15"
                  BindingContext="{Binding BodyPart , Source={x:Reference QuickBodyPartViewControl}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Entry FontSize="22" HorizontalTextAlignment="End" HorizontalOptions="Fill" Grid.Column="0" Keyboard="Numeric" VerticalOptions="Center" Text="{Binding CurrentHitpoints}"></Entry>
                <Label FontSize="22" Grid.Column="1" VerticalOptions="Center" Text="/"/>
                <Label FontSize="22" HorizontalOptions="Center"  Grid.Column="2" VerticalOptions="Center" Text="{Binding MaxHitpoints}" />
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>