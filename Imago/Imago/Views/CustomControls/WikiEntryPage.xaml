<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Imago.Models;assembly=Imago"
             xmlns:viewModels="clr-namespace:Imago.ViewModels;assembly=Imago" Background="white"
             Title="{Binding WikiPageEntry.Title}"
             x:Class="Imago.Views.CustomControls.WikiEntryPage" x:DataType="viewModels:WikiEntryPageViewModel">
    <ContentPage.Content>
        <Grid Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Button Text="Schließen" Command="{Binding ClosePageCommand}"/>
                <Entry IsReadOnly="True" Text="{Binding WikiPageEntry.Url}" HorizontalOptions="FillAndExpand"></Entry>
            </StackLayout>

            <Frame Grid.Row="1">
                <WebView Navigating="WebView_OnNavigating" Navigated="WebView_OnNavigated" Margin="-19" Source="{Binding WikiPageEntry.Url}" />
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>