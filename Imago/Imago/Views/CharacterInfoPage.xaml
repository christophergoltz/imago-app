<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Imago.ViewModels;assembly=Imago"
             xmlns:customControls="clr-namespace:Imago.Views.CustomControls;assembly=Imago"
             xmlns:models="clr-namespace:Imago.Models;assembly=Imago"
             Title="{Binding Title}"
             x:Class="Imago.Views.CharacterInfoPage"
             BindingContext="{Binding CharacterInfo, Source={StaticResource ViewModelLocator}}"
             x:DataType="viewModels:CharacterInfoPageViewModel">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="500"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <TableView Grid.Column="1" Grid.Row="0" Grid.RowSpan="2">
                <TableRoot>
                    <TableSection Title="Rasse">
                        <TextCell Text="{Binding Character.RaceType}"/>
                    </TableSection>
                </TableRoot>
            </TableView>

            <Frame Grid.Column="0" Grid.Row="0" BorderColor="Black" CornerRadius="0">
                <CollectionView ItemsSource="{Binding AttributeViewModels}" VerticalOptions="Start" VerticalScrollBarVisibility="Never">
                    <CollectionView.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="1"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Text="Attribut" Margin="5,0,0,0" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="1" Text="NW" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="2" Text="SW" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="3" Text="EP" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="4" Text="Korr" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="5" Text="FW" FontAttributes="Bold"/>
                            <BoxView Grid.Column="0" HeightRequest="1" Grid.Row="1" Grid.ColumnSpan="6" Color="Black"/>
                        </Grid>
                    </CollectionView.Header>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:AttributeViewModel">
                            <Grid Padding="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" VerticalOptions="Center" Text="{Binding Attribute.Type}"/>
                                <Label Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Attribute.NaturalValue}"/>
                                <Label Grid.Column="2" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Attribute.IncreaseValue}"/>
                                <Label Grid.Column="3" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Attribute.Experience}"/>
                                <Entry Grid.Column="4" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Attribute.Corrosion}"/>
                                <Label Grid.Column="5" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Attribute.FinalValue}"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>

            <Frame Grid.Column="0" Grid.Row="1" CornerRadius="0" BorderColor="Black">
                <CollectionView ItemsSource="{Binding Character.Professions}">
                    <CollectionView.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="50" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="1"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Text="Fähigkeit" Margin="5,0,0,0" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="1" Text="Formel" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="2" Text="NW" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="3" Text="Mod" FontAttributes="Bold"/>
                            <Label Grid.Row="0" Grid.Column="4" Text="FW" FontAttributes="Bold"/>
                            <BoxView Grid.Column="0" HeightRequest="1" Grid.Row="1" Grid.ColumnSpan="5" Color="Black"/>
                        </Grid>
                    </CollectionView.Header>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Profession">
                            <Grid Padding="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" VerticalOptions="Center" Text="{Binding Type}"/>
                                <Label Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding Formula}"/>
                                <Label Grid.Column="2" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding NaturalValue}"/>
                                <Entry Grid.Column="3" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding ModificationValue}"/>
                                <Label Grid.Column="4" VerticalOptions="Center" HorizontalOptions="Center" Text="{Binding FinalValue}"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>
        </Grid>
    </ContentPage.Content>
</ContentPage>