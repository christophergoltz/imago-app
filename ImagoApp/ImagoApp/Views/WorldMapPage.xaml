<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             xmlns:customControls="clr-namespace:ImagoApp.Views.CustomControls;assembly=ImagoApp"
             x:Class="ImagoApp.Views.WorldMapPage">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>-->
        <customControls:CustomSKCanvas 
                                       BackgroundColor="{StaticResource AntiUmbra3}" 
                                       x:Name="DrawingSKCanvas" 
                                       PaintSurface="SKCanvasView_OnPaintSurface">
        </customControls:CustomSKCanvas>

        <StackLayout Padding="20" Grid.Row="0" Orientation="Horizontal" BackgroundColor="{StaticResource GruenesUmbra2}" VerticalOptions="Start" HorizontalOptions="FillAndExpand">
            <Label FontSize="22" Text="AnchorX"></Label>
            <Label FontSize="22" Text="{Binding AAnchorX, Source={x:Reference DrawingSKCanvas}}"></Label>
               
            <Label FontSize="22" Margin="10,0,0,0" Text="AnchorY"></Label>
            <Label FontSize="22" Text="{Binding AAnchorY, Source={x:Reference DrawingSKCanvas}}"></Label>
           
            <Label FontSize="22" Margin="10,0,0,0" Text="StartX"></Label>
            <Label FontSize="22" Text="{Binding StartX, Source={x:Reference DrawingSKCanvas}}"></Label>
         
            <Label FontSize="22" Margin="10,0,0,0" Text="StartY"></Label>
            <Label FontSize="22" Text="{Binding StartY, Source={x:Reference DrawingSKCanvas}}"></Label>
               
            <Label FontSize="22"  Margin="10,0,0,0" Text="Zoom"></Label>
            <Label FontSize="22" Text="{Binding CurrentScale, Source={x:Reference DrawingSKCanvas}}"></Label>
        </StackLayout>


    </Grid>
   
</ContentPage>